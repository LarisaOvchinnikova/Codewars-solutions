//https://www.codewars.com/kata/57b9fc5b8f5813384a000aa3

function calculate(str) {
  let nums = str.split(" ").filter(el => !isNaN(el));
  return str.includes("loses") ? +nums[0] - +nums[1] : +nums[0] + +nums[1]
}
