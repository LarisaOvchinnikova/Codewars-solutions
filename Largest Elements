//https://www.codewars.com/kata/53d32bea2f2a21f666000256/train/javascript
function largest(n, arr) {
  arr = arr.sort((m,l) => m-l);
  let ar = [];
  for (let i = arr.length - n; i < arr.length; i++)
  {
    ar.push(arr[i]); 
  }
  return ar;
}
